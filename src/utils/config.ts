// Central configuration for security and authentication defaults
// WARNING: This value MUST be kept in sync with the backend's settings.DEFAULT_TEMP_PASSWORD.
// If the backend default temporary password is changed, update this constant accordingly to prevent credential mismatch.
export const DEFAULT_TEMP_PASSWORD = 'IqraSecurePass2026!';
